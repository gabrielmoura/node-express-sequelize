<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Teste de Página</title>
    <script src="/app.js"></script>
</head>
<body>
<%- include('../../layout/navbar') %>
<div class="container">
    <h3><%= category.title %></h3>
    <% articles.forEach((article)=>{ %>
        <div class="card">
            <div class="card-header">
                <%= article.title %>
            </div>
            <div class="card-body">
                <a href="/admin/article/<%= article.slug %>" class="btn btn-success">Ler artigo</a>
            </div>
        </div>
    <% }) %>

</div>
</body>
</html>